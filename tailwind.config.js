/** @type {import('tailwindcss').Config} */
module.exports = {
    content: ['./src/**/*.{html,ts}'],
    theme: {
        extend: {},
        fontFamily: {
            headingline: ['Bebas Neue', 'sans-serif'],
            title: ['Cabin', 'sans-serif'],
            body: ['Nunito', 'sans-serif'],
        },
    },
    plugins: [],
}
